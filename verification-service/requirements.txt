fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pillow==10.4.0
opencv-python-headless==4.10.0.84
# Prebuilt dlib wheel — installs in seconds instead of compiling from source.
# face_recognition is installed with --no-deps in the Dockerfile so it reuses
# this dlib + the model files below (avoids pulling the source `dlib` package).
dlib-bin==19.24.6
face_recognition_models==0.3.0
click==8.1.7
pytesseract==0.3.13
unidecode==1.3.8
python-Levenshtein==0.25.1
pydantic==2.9.2
pydantic-settings==2.5.2
numpy==1.26.4
httpx==0.27.2
# Tests
pytest==8.3.3
pytest-asyncio==0.24.0
