# Demo

Visual proof that EspritConnect runs. **Add the following before submitting** (the
ESPRIT publication guide requires screenshots/GIF or a video link):

## To add here
- `screenshots/` — PNGs of the main screens:
  - `landing.png` — the public landing page
  - `feed.png` — the feed/timeline
  - `directory.png` — the alumni directory
  - `profile.png` — a profile page
  - `admin.png` — the admin dashboard
  - `resources.png` — the resources page
- `demo.mp4` **or** a video link below — a 1–3 min walkthrough
  (login → feed → directory → jobs/events → admin moderation).

## Video
- Walkthrough: <add YouTube / Google Drive link here>

## Live demo (optional)
- URL: <add a tunnel/deploy link here, e.g. https://…trycloudflare.com>

> Tip: the app fills with rich seed data on first boot, so the screenshots/video
> look populated immediately. Log in with a seeded account (`Test@1234`).
